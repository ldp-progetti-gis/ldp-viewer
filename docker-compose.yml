version: '3.3'
services:
        web:
                build:
                        context: ./php
                        dockerfile: Dockerfile
                container_name: php73new
                volumes:
                        - ./php:/var/www/html/
                ports:
                        - 9000:80
